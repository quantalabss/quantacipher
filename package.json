{
  "name": "quantacipher-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "quantacipher-web",
    "quantacipher-gateway",
    "quantacipher-sdk-js",
    "quantacipher-wasm/pkg"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
